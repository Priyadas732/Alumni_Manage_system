# Frontend Environment Variables
# Copy this to .env for local development

# Backend API URL — leave empty if using Vite proxy (default for local dev)
# In production, the backend serves the frontend, so no URL is needed
VITE_API_URL=
